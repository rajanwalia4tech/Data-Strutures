struct arrayStack* createStack(int );
int isFull(struct arrayStack* );
int isEmpty(struct arrayStack* );
void Push(int data,struct arrayStack* );
int Pop(struct arrayStack* );
void view(struct arrayStack* );
